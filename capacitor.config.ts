import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.techishare',
  appName: 'techishare',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
